spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/tareas
    username: usuario
    password: contraseña
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update